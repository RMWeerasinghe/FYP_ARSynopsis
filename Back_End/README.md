install dbcv (Fast Density-Based Clustering Validation (DBCV))

`python -m pip install "git+https://github.com/FelSiq/DBCV"`

`pip install streamlit`

run app `streamlit run app.py`

`pip install fastapi`

pip install uvicorn

uvicorn main:app --reload